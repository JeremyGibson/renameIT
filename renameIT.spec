# -*- mode: python -*-
a = Analysis(['renameIT.py'],
             pathex=['C:\\Users\\jgibson\\PycharmProjects\\RenameIT'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='renameIT.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='renameIT')
